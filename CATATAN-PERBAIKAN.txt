PERBAIKAN v17.2 — 28 Agustus 2026
==================================

Cukup timpa 2 file ini di repo, lalu push/deploy:

  ops.js              -> di folder utama (root)
  api/operations.js   -> di folder api/

YANG BERUBAH
------------
1. Panel Admin -> Operasi: bagian "Responder Aktif" dan tombol
   "Instruksi" dihapus (fitur responder memang sudah dimatikan dari
   sisi pendaki, jadi bagian ini tidak akan pernah terisi).
2. Server: endpoint sos-report dan sos-instructions ditutup
   (tidak ada lagi yang memakainya).
3. Dark mode: dropdown check-in Pos dan kolom input QR sekarang
   memakai warna tema (latar gelap, huruf terang) — sebelumnya putih
   polos sehingga hurufnya tidak terbaca.

OPSIONAL (tidak wajib)
----------------------
Tabel sos_responders di Supabase boleh dihapus:
  drop table if exists sos_responders;

CEK CEPAT SETELAH DEPLOY
------------------------
- Panel Admin -> Operasi: hanya ada SOS Aktif, Riwayat SOS,
  Check-in Terakhir.
- Dark mode -> bagian Check-in Pos: tulisan Pos 1..Puncak terbaca.
