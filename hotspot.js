/**
 * hotspot.js — Catatan pengembang
 *
 * File ini BUKAN endpoint API aktif.
 * Endpoint hotspot titik panas yang sesungguhnya ada di: api/hotspot.js
 *
 * Vercel hanya menjalankan handler dari folder `api/`.
 * File di root ini tidak dieksekusi sebagai serverless function.
 *
 * Sebelumnya file ini berisi kode api/bmkg.js secara keliru (konten tertukar).
 * Bug tersebut sudah diperbaiki — kode BMKG tetap berada di api/bmkg.js.
 */
