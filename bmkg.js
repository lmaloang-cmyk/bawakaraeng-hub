/**
 * bmkg.js — Catatan pengembang
 *
 * File ini BUKAN endpoint API aktif.
 * Endpoint data cuaca BMKG yang sesungguhnya ada di: api/bmkg.js
 *
 * Vercel hanya menjalankan handler dari folder `api/`.
 * File di root ini tidak dieksekusi sebagai serverless function.
 *
 * Sebelumnya file ini berisi konfigurasi JSON vercel (yang berbeda dari vercel.json aktif)
 * secara keliru — konten tersebut tidak pernah dipakai Vercel karena formatnya salah
 * dan bukan file bernama vercel.json. Konfigurasi Vercel yang aktif ada di: vercel.json
 */
