/* Heartbeat GPS+baterai saat aplikasi aktif dan pemulihan setelah layar menyala. */
(function(w){'use strict';var timer=null,busy=false;function active(){return w._tkSession&&w._tkSession.id;}function pulse(force){if(!active()||busy||!navigator.geolocation)return;busy=true;navigator.geolocation.getCurrentPosition(function(p){var run=function(b){if(force)w._tkLastSentAt=0;if(typeof w._tkSendPos==='function')w._tkSendPos(p.coords.latitude,p.coords.longitude,p.coords.accuracy,b,p.coords.altitude);busy=false;};if(typeof w._tkReadBattery==='function')w._tkReadBattery().then(run).catch(function(){run(null);});else run(null);},function(){busy=false;},{enableHighAccuracy:true,timeout:18000,maximumAge:force?0:15000});}function start(){clearInterval(timer);pulse(true);timer=setInterval(function(){if(!document.hidden)pulse(false);},30000);}document.addEventListener('visibilitychange',function(){if(!document.hidden)start();});w.addEventListener('pageshow',start);w.addEventListener('online',function(){pulse(true);});setTimeout(start,1200);})(window);
